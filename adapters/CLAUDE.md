@AGENTS.md

Project rules live in `AGENTS.md` at the repository root, imported above. On
conflict, `AGENTS.md` prevails. Run its `init` and `context` commands (see
`AGENTS.md`) before coding, and `claim`/`pause`/`done` to take and close
tasks.
